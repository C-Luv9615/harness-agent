---
description: TDD implementation of a specific task
---
$ARGUMENTS

Before first implementation task: check if working in an isolated worktree (see CLAUDE.md TDD execution rules). If not, offer to create one.

Then execute TDD for the specified task: experience search → read interface → write test RED → minimal implementation GREEN → refactor → verify against design. Follow CLAUDE.md TDD execution rules.
