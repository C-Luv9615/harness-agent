---
description: Quick fix path — skip SDD, keep review/verify
---
$ARGUMENTS

Use the fix path: experience search → TDD implement → verify → review → commit → lessons. Follow CLAUDE.md fix path rules.
