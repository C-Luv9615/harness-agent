---
description: Full workflow — execute all phases with pause at each stage
---
Run the complete workflow sequentially: init → specify → clarify → plan → tasks → implement → verify → review → commit → evolve. Pause for user confirmation at each phase transition. Follow CLAUDE.md rules.
