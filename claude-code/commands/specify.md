---
description: Full SDD path — spec → clarify → plan → tasks → implement
---
$ARGUMENTS

Use the specify path: SDD planning (mispec or spec-design) to generate .specify/{feature}/spec.md. Then continue with clarify → plan → tasks. Worktree and TDD happen at implement phase. Follow CLAUDE.md specify path rules.
