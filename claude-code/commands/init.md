---
description: Initialize .harness/ and .specify/ project structure
---
Create `.harness/` directory with PROGRESS.md, lessons-learned.md, verify.sh. If mispec CLI is available, run `mispec init . --ai claude --force --no-git` to initialize `.specify/`. Follow the init details in CLAUDE.md.
